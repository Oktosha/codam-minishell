name: Norm
run-name: norminette running
on: [pull_request]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: python3 -m pip install --upgrade pip setuptool
      - run: python3 -m pip install norminette
      - run: ./tests/norm.sh
      - run: echo "🍏 This job's status is finalized."
